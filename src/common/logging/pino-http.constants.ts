/** Texto con el que Pino sustituye headers de auth/firma. */
export const PINO_REDACT_CENSOR = "[redacted]";

/** File descriptor de stdout; `pino/file` con destination 1 escribe JSON a consola. */
export const PINO_STDOUT_FD = 1;
